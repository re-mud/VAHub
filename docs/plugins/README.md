# Плагины
Плагины служат для добавления команд, которых изначально нет в программе.

## Создание
Создайте папку в `Plugins` с файлом `manifest.json`:
```json
{
	"name": "имя_плагина",
	"description": "описание",
	"version": "версия",
	"type": "тип",
	"commands": {
		"текст": "данные"
	}
}
```

Пример заполенного манифеста в [Пример манифеста](#пример-манифеста).

## Манифест

### Type
Обязательное поле, определяет обработчик плагина.  
Подробнее в [Типы плагинов](#типы-плагинов).

### Commands
Словарь, где ключи используются при распознавании, для указания нескольких команд используйте разделитель `|` например:
`"запусти браузер|открой браузер"`.  
Данные под ключами задают поведение команды и зависят от [типа](#type) плагина.

### Пример манифеста
*пример для типа `program`*
```json
{
	"name": "МойПлагин",
	"description": "Описание плагина",
	"version": "1.0",
	"type": "program",
	"commands": {
		"запусти браузер|открой браузер": "scripts/open_browser.bat",
		"закрой браузер": "scripts/close_browser.bat"
	}
}
```

## Возврат команд
В типах с возвратом данных возможно вернуть json-строку в формате:
```json
{
	"action": "тип",
	"speak": "текст"
}
```

- action - тип действия программы (None/Close) 
- speak - текст который будет озвучен пользователю

## Типы плагинов
Подробные примеры:
- [Program](program.md)
- [Python](python.md)