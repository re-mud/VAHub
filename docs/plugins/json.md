# Json-плагин
Используется для отладки возврата или простейших команд программе.


## Манифест
В `manifest.json` укажите тип:
```json
"type": "json"
```


## Команды
В поле `commands` из `manifest.json` возврат команд пишется напрямую, пример:
```json
"выход": "{\"action\": \"close\"}"
```